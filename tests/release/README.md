# Cross-component release fixtures

Use this directory only for black-box fixtures that span server, web, infra, and
operations authorities. Component-specific release tests remain with their
component or under `ops/tests`.
