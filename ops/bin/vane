#!/usr/bin/env python3
"""Thin bootstrap for the repository's sole operator-facing CLI."""

from pathlib import Path
import sys


ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT))

from ops.cli.controller import main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(main())
